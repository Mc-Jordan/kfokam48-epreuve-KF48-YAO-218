# D4 — États-transitions : cycle de vie d'un exercice (bonus +3)

> Facultatif, noté en bonus. Déposé → en attente de relecture → relu.
> Doit rester cohérent avec le champ `statut` renvoyé par `POST /api/exercices`
> et avec la règle Q13 (remplacement du lien tant que la relecture n'a pas commencé).

```mermaid

```

**Règles de gestion illustrées :** <RGx, …>
