/**
 * Écran formateur (contrainte F2) : ouvrir une session, suivre le tableau.
 * Squelette posé par le socle technique ; rempli par les tickets #3, #6, #9, #10 et #12.
 */
export default function EcranFormateur() {
  return (
    <section>
      <h1>Formateur</h1>
      <p>Ouvrir une session, clôturer, finaliser, consulter le tableau.</p>
    </section>
  );
}
